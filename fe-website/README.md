# Admin Website

Separate web admin client for the Movie Ticket Booking backend.

## Run

```bash
cd fe-website
npm install
npm run dev
```

## Environment

Create `.env` if needed:

```bash
VITE_API_BASE_URL=http://localhost:3000/api
```

If not set, the app defaults to `http://localhost:3000/api`.
